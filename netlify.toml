[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."pdf-extractor"]
  external_node_modules = ["pdf-parse"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
